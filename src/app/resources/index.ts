// import a pre-defined template for config and content options
import { routes, protectedRoutes, effects, style, display, mailchimp, baseURL } from "./config";
import { person, social, newsletter, home, about, work, gallery } from "./content";

export {
  routes,
  protectedRoutes,
  effects,
  style,
  display,
  mailchimp,
  baseURL,
  person,
  social,
  newsletter,
  home,
  about,
  work,
  gallery
};
